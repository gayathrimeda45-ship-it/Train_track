services:
  - type: web
    name: train-track
    env: static
    branch: main
    buildCommand: ""   # no build needed for plain HTML/CSS/JS
    staticPublishPath: .  # publish everything from repo root
    plan: free